interface:
  display_name: "AI复利系统"
  short_description: "把每天的 AI 协作复盘成审批台、规则候选、Skill 候选、文稿候选和数字资产建议，并用审批反馈持续校准"
  default_prompt: "Use $ai-compounding-system. If this is the first run, show the value introduction first, then guide setup one question at a time. Otherwise review one specified day of AI work and generate an approval workbench."
