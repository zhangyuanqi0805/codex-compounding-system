# AI 复利系统配置模板

如果你想手动保存配置，可以把这份模板放到 Codex/Code Desk 工作台的配置文件里，例如：

```text
<CODE_DESK_WORKBENCH>/.ai-compounding-system/config.md
```

## 基础信息

- 用户称呼：
- 默认时区：Asia/Shanghai
- 数字资产库目录：
- Codex/Code Desk 过程产物目录：默认由 Codex 在当前工作台下创建 `AI复利系统/过程产物/`
- 复盘输出目录：默认跟随过程产物目录
- 默认复盘时间：每天凌晨或手动触发

## 证据来源

- Codex/Code Desk 工作记录：允许 / 不允许
- 本地过程产物：允许 / 不允许
- 本地文件变更：允许 / 不允许
- 云端文档链接：只记录链接 / 授权后读取 / 不使用
- 截图：默认不截图 / 审批后补截图 / 每次都截图

## 审批偏好

- 审批台形式：默认本地 HTML 审批台
- 默认动作是否可预选：是 / 否
- 备注优先级：备注高于按钮 / 按钮高于备注
- 审批结果回传方式：复制 JSON / 下载 JSON / 直接对话说明

## 数字资产规则

- 写初稿是否等于入库：否
- 入库是否必须单独确认：是
- 入库文件名是否带日期：是
- 默认入库目录：

## 个性化写作偏好

- 喜欢的文章风格：
- 不喜欢的文章风格：
- 标题偏好：
- 是否需要底部证据索引：
